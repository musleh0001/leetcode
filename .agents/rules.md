# Permanent Project Rules

## LeetCode Solution Guardrail
Whenever creating or editing files inside the `problems/` directory:
- **NEVER WRITE SOLUTION LOGIC.**
- The body of every method inside `class Solution` MUST strictly contain only `pass` or a docstring followed by `pass`.
- If you write implementation logic, loop logic, or return statements inside a solution method, you violate workspace constraints.