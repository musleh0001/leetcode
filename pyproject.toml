[project]
name = "leetcode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyrefly>=1.2.0",
    "pytest>=9.0.3",
    "ruff>=0.15.11",
]

[tool.ruff.lint]
select = ["I"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-vs"

[tool.pyrefly]
project-includes = [
    "**/*.py*",
    "**/*.ipynb",
]
